# bridges/inbox

Questions Claude writes for another model to answer (because Claude can't access the PII / sensitive data the question requires).

You take these to the target model manually. Naming: `<MODEL>-<YYYY-MM-DD>-<n>.md`.

See `../README.md` for the full pattern.
