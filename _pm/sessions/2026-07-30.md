# 2026-07-30

## Intent

Stand-up day — retro-written, since the project was born mid-session. The day began as a library ingest of Matt Pocock's AI Hero material; it became: stand up Tiamat as a light tracking fork of Ringer, migrate everything load-bearing out of Agent-Argus, and prove the fork end-to-end. Scope grew twice on Joe's direct asks: implement the Ralph loop, then build the Den. Done-for-today: fork current with upstream, all engine lanes proven live, both new capabilities working and pushed.

## Shipped

Eleven fork deltas, all pushed to `datacraftdevelopment/ringer` main (`git log --oneline upstream/main..main` is the ledger):

- `2b6637b` — TIAMAT.md identity + fork doctrine; ralph-loop evolution flag (`docs/tiamat/evolutions/`)
- `5174842` — `engines/claude-worker.sh` (subscription Claude lane, migrated from Agent-Argus)
- `feab825` — adversarial-review-panel kit + model notebook (migrated)
- `7b1e105` — `templates/ralph-loop/` (ralph_loop.py + tests + README + example PRD)
- `b34b2f6` — README contributor credit (their audit test caught the fork's own deltas)
- `f368e7a` — Ringside face selection `/?face=<name>` + tests + live-captured API fixtures
- `25d9b87` — `dashboard/faces/den.html` (the Den)
- `e13dc38` — ralph-loop test eval-log isolation
- `8fb7347` — Den evolution doc: built, v1 live
- (plus this _pm scaffold fill and the TIAMAT.md `_pm/` note, committed today)

Environment: fork cloned to `Agent/Agent_Tiamat`, `upstream` remote wired push-disabled, fast-forwarded `4ac3791 → a1a91b8`; `~/.config/ringer/config.toml` repointed (backup kept); ringer + cross-review-gate skills repointed in `_Core/library` (`86eb08e`, `302769c`).

## Tried / Learned / Decided

- **Proofs, in order:** upstream suite 253/253 on the fresh sync → three-lane engine smoke 3/3 first-try (codex/claude/opencode) → ralph-loop self-test 3/3 on the mock engine → live Ralph run: codex built `greet.py`, haiku extended it in a worktree containing iteration 1's commit, `<promise>COMPLETE</promise>` → face tests 8/8 → full suite 261/261.
- **Ralph design pivot:** direct-repo editing fights the worker sandbox (`-C taskdir` + workspace-write). Went worktrees mode: worker edits an isolated checkout, the check exports a patch, the *loop* applies + commits. Failed iterations can't touch the repo by construction. See decision: [2026-07-30 — light tracking fork](../decisions/2026-07-30-light-tracking-fork.md) for the structural frame.
- **Their culture bites back, pleasantly:** the contributor-audit test failed the suite until the fork credited its own author in the README. The Den 404s on unknown faces instead of silently falling back, because explicit selection deserves an explicit answer — same spirit.
- **Corrected mid-flight:** briefly misread historical mock rows (July-21 `migration-smoke`) as unit-test pollution of the scoreboard — false; fixtures were honest. Kept the defensive eval-log isolation in the kit's test config anyway. Also: the (untyped) scoreboard bucket traced to the morning's engine smoke lacking `task_type: probe` — my miss, now a Backlog hygiene item; Ralph tasks type themselves `code-feature`.
- **Upstream review lane opened on day one:** the `4ac3791 → a1a91b8` gap shipped an `ask` command, a context-packet system (compare with the token-saver skill's bundled version), a bakeoff-kit, and 64 changed lines in their in-repo ringer skill (our global skill needs re-reconciling).

## Intent vs. outcome

Hit and exceeded: the fork stands, proven live, with two working capabilities (Ralph loop, Den) that were only evolution flags at breakfast. Deliberate scope growth, owner-driven both times. Drift to watch: day one built *two* things from the evolutions queue on direct asks — fine for stand-up day, but the queue's do-not-build-until-demand rule starts binding now.

## Open threads

- The real-backlog Ralph proving run is the gate on the upstream template PR — the cloud-agents experiment project is the candidate (TASKS: Current).
- Upstream PR (a) — face selection — is ready material; decide when to send.
- Argus teardown checklist untouched today (deliberate; nothing load-bearing points there anymore).
- `_pm/` now rides in a **public fork** — keep entries clean of anything client-flavored (all-Ringer content is fine).
